# Logs
logs
*.log
npm-debug.log*

# Dependency directories
node_modules/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# dotenv environment variables file
.env

# Production files
# NOTE: dist IS committed in this repo — jsDelivr serves the bundle straight
# from GitHub, so an ignored dist/ means every pinned CDN URL 404s.
# Only the local smoke-test harness stays out.
dist/test.html
dist/nav-test.html
dist/team-test.html