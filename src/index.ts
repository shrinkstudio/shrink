// Shrink Studio — site bundle entry point.
//
// Starting from scratch: this bundle is the behaviour layer only. Lumos owns
// classes and CSS in Webflow (see .claude/skills/lumos-skill). Add modules per
// section as the build progresses and initialise them inside the Webflow.push
// callback below.

window.Webflow ||= [];
window.Webflow.push(() => {
  // Modules initialise here.
});
